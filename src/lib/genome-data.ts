/**
 * Bundled Future2 Experience Genome — the AUTHORITATIVE production Core Library.
 *
 * This local export is the source of truth and is always available with zero
 * runtime dependency (no Sheets API, no network). Paste the full exported Genome
 * below (all worksheets), e.g. as blocks like:
 *   === WORKSHEET (collection): <sheet name> | columns: <c1, c2, ...> ===
 *   [<sheet name> row 2] c1: ... | c2: ...
 * Opt-in live sync (GENOME_LIVE_SYNC=1) only augments this and silently falls
 * back here if the live workbook is unavailable — generation is never blocked.
 */
export const GENOME_TEXT = String.raw``;