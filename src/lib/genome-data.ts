/**
 * Bundled Future2 Experience Genome text — a permanent Core Library resource.
 *
 * Paste the exported Genome here (CSV or plain text, one pattern per row/block)
 * to bundle it into the app so it loads with zero runtime dependency. If this is
 * left empty, the Core Library falls back to fetching GENOME_CSV_URL at runtime
 * (the Genome sheet published to the web as CSV). If neither is available,
 * lesson generation fails with a clear error.
 */
export const GENOME_TEXT = "";